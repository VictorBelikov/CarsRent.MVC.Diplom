@model CarsRentEF.Models.Discount

@{
    ViewBag.Title = "Discount Delete";
}

<div class="well well-sm"><h1>Удалить скидку</h1></div>
<h3>Вы уверены, что хотите удалить?</h3>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.НаименованиеСкидки)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.НаименованиеСкидки)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Процент)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Процент)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        @Html.HiddenFor(x => x.СкидкаID)
        @Html.HiddenFor(x => x.Timestamp)
        <div class="form-actions no-color">
            <button type="submit" class="btn btn-danger">
                <span class="glyphicon glyphicon-trash"></span>&nbsp;Удалить
            </button>
            &nbsp;&nbsp;
            <a href="@Url.Action("Index")" class="btn btn-default">
                <span class="glyphicon glyphicon-list"></span>&nbsp;К списку
            </a>
        </div>
    }
</div>
