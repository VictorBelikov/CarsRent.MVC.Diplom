@model CarsRentEF.Models.Discount

@{
    ViewBag.Title = "Discount Details";
}

<div class="well well-sm"><h1>Данные по скидке</h1></div>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.НаименованиеСкидки)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.НаименованиеСкидки)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Процент)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Процент)<span> %</span>
        </dd>

    </dl>
</div>
<p>
    <a href="@Url.Action("Edit", new {id = Model.СкидкаID})" class="btn btn-warning">
        <span class="glyphicon glyphicon-edit"></span>&nbsp;Редактировать
    </a>
    &nbsp;&nbsp;
    <a href="@Url.Action("Delete", new {id = Model.СкидкаID})" class="btn btn-danger">
        <span class="glyphicon glyphicon-trash"></span>&nbsp;Удалить
    </a>
    &nbsp;&nbsp;
    <a href="@Url.Action("Index", new {id = Model.СкидкаID})" class="btn btn-default">
        <span class="glyphicon glyphicon-list"></span>&nbsp;К списку
    </a>
</p>
